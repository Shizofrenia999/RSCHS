<Window x:Class="RSCHS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RSCHS"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <DockPanel>

        <Border DockPanel.Dock="Top" Background="#FF3333" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="МЧС РОССИИ - СИСТЕМА ДИСПЕТЧЕРА" 
                          Foreground="White" FontSize="16" FontWeight="Bold"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Диспетчер:" Foreground="White" Margin="0,0,5,0"/>
                    <TextBlock x:Name="txtDispatcherName" Foreground="White" FontWeight="Bold"/>
                    <Button x:Name="btnLogout" Content="Выйти" Margin="10,0,0,0" 
                           Click="BtnLogout_Click" Background="#FFD700"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Меню -->
        <TabControl x:Name="MainTabControl">
            <TabItem Header="Происшествия">
                <!-- Здесь будет список происшествий -->
            </TabItem>

            <TabItem Header="Новое происшествие">
                <!-- Здесь будет форма создания происшествия -->
            </TabItem>

            <!-- ДОБАВЬ ЭТУ ВКЛАДКУ -->
            <TabItem Header="Назначить вызов">
                <!-- Содержимое будет создано в коде -->
            </TabItem>

            <TabItem Header="Назначения">
                <!-- Здесь будет управление назначениями -->
            </TabItem>

            <TabItem Header="Отчеты">
                <!-- Здесь будет работа с отчетами -->
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>