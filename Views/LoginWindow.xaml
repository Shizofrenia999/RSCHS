<Window x:Class="RSCHS.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RSCHS.ViewModels"
        mc:Ignorable="d"
        Title="Вход в систему МЧС" 
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="МЧС РОССИИ" FontSize="20" FontWeight="Bold" 
                      HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Label Content="Логин:"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Height="25"/>

            <Label Content="Пароль:" Margin="0,10,0,0"/>
            <PasswordBox x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" 
                        Height="25"/>

            <Button Content="Войти" Command="{Binding LoginCommand}" 
                   Background="#FF3333" Foreground="White" 
                   FontWeight="Bold" Height="30" Margin="0,20,0,0"/>

            <Button Content="Регистрация" Command="{Binding RegisterCommand}" 
                   Margin="0,10,0,0" Height="25"/>
        </StackPanel>
    </Grid>
</Window>